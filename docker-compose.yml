version: '3.8'

services:
  whisper-timestamped:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whisper-timestamped
    restart: unless-stopped
    volumes:
      - /home/vlad/data/test_data/:/app/input
    # - ./input/:/app/input
      - ./output:/app/output
      - ./cache:/app/cache
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            device_ids: ['0']
            capabilities: [gpu]